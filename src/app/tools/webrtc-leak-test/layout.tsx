import type { Metadata } from "next";
export const metadata: Metadata = { title: "WebRTC Address Test — Evidence-Based IP Check", description: "Inspect addresses visible through WebRTC, compare them with your detected IP, and understand whether the evidence needs review.", alternates: { canonical: "https://browserprobe.app/tools/webrtc-leak-test" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
