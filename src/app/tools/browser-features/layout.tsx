import type { Metadata } from "next";
export const metadata: Metadata = { title: "Browser Features Support — API Compatibility Test", description: "Test your browser's support for 30+ modern web APIs including WebSocket, Push Notifications, Clipboard API, and more. Free compatibility checker.", alternates: { canonical: "https://browserprobe.app/tools/browser-features" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
